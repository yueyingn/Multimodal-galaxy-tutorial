"""Minimal AION package for the Gal4M codec tutorial.

The full AION package exposes the FourM transformer via ``from .model import AION``.
The codec tutorial only needs the modality definitions and the codec stack, so
that heavy import is intentionally omitted here to keep the tutorial light.
"""
